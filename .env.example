# === Required ===
DATABASE_URL=postgresql://gsim:CHANGE_ME@localhost:5432/gsim
NODE_ENV=production
PORT=5000

# === Optional (express-session, если будет включён) ===
# SESSION_SECRET=$(openssl rand -hex 32)
